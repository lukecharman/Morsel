import SwiftUI

struct StatsView: View {
  var body: some View {
    VStack {
      Text("Stats view incoming...")
        .padding(.top, 48)
      Spacer()
    }
  }
}

#Preview {
  StatsView()
    .background(Color(.systemGroupedBackground))
}
